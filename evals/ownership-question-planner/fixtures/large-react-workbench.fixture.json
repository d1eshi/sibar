{
  "fixture_id": "LARGE-REACT-WORKBENCH",
  "label": "Large App-style React workbench file with repeated ownership slices",
  "selected_file_path": "evals/ownership-question-planner/fixtures/large-react-workbench.tsx",
  "provider_id": "fixture-large-react-workbench",
  "question_budget": 10,
  "generated_from": "apps/sibar-research-workspace/src/App.tsx",
  "append_repeat_count": 12,
  "append_block": [
    "export function SyntheticWorkbenchSegment(segmentId: number) {",
    "  const ownsCurrentSegment = segmentId % 2 === 0;",
    "  const label = ownsCurrentSegment ? \"synthesis\" : \"handoff\";",
    "  return {",
    "    id: `segment-${segmentId}` as const,",
    "    label,",
    "    ownsCurrentSegment,",
    "  };",
    "}",
    "",
    "export function syntheticApiBoundary(payload: string) {",
    "  return fetch(`/api/ownership/${payload}`).then((response) => response.json());",
    "}",
    "",
    "export function useSyntheticTracker(tag: string) {",
    "  React.useEffect(() => {",
    "    void tag;",
    "  }, [tag]);",
    "}",
    "",
    "// Synthetic workbench ownership guard for repeated local slices",
    "export function syntheticRepairPath(segmentId: number) {",
    "  const routeSignal = `route-${segmentId}`;",
    "  useSyntheticTracker(routeSignal);",
    "  if (!Number.isFinite(segmentId)) {",
    "    return { segmentId, ready: false } as const;",
    "  }",
    "  return { segmentId, ready: segmentId >= 0, route: routeSignal };",
    "}",
    ""
  ],
  "user_intent": "Validate local ownership and repair boundaries inside one large React workbench file.",
  "evidence_markers": [
    { "id": "excerpt-header-imports", "token": "import", "span_before": 1, "span_after": 15 },
    { "id": "excerpt-app-entry", "token": "export default function App()", "span_before": 2, "span_after": 10 },
    { "id": "excerpt-open-flow", "token": "function openFlowStep", "span_before": 1, "span_after": 8 },
    { "id": "excerpt-readiness-copy", "token": "readinessPendingCopy", "span_before": 1, "span_after": 6 },
    { "id":  "excerpt-tail-segment", "token": "SyntheticWorkbenchSegment", "span_before": 1, "span_after": 12 },
    { "id": "excerpt-tail-boundary", "token": "syntheticApiBoundary", "span_before": 1, "span_after": 8 },
    { "id": "excerpt-tail-repair", "token": "syntheticRepairPath", "span_before": 1, "span_after": 10 }
  ]
}
