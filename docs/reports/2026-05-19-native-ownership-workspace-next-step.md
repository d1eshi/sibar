# Native Ownership Workspace Next Step

Este worktree es `/Users/d1eshi/.codex/worktrees/26e5/sibar` y quedó en `detached HEAD` sobre el commit `2db5cc02c11a6664245c1eae96db4d3a5a402689` (`2db5cc0 fix: render live workspace artifact previews`). Este commit cierra la primera base reproducible del workspace nativo Sibi/Sibar: `swift run SibiStudyApp`, click en `Start`, contrato runtime renderizable, tres paneles SwiftUI, artifact/code preview, evidencia seleccionable, intento del usuario, submit vía `submit_workspace_attempt`, evaluación determinística, gap, repair y scoped readiness dentro de la UI.

Lo que apunta este branch/worktree es probar que Sibi no es un chat al lado del código ni un editor genérico, sino un ownership loop nativo: el usuario ve el boundary del repo, entiende qué slice está mirando, selecciona evidencia, intenta primero, y recién después recibe diagnóstico y reparación. La reproducción determinística vive en `evals/deep-ownership-workspace/fixtures/live-workspace-session.json` y se puede correr con `SIBI_WORKSPACE_FIXTURE_MODEL_RESPONSE_PATH=evals/deep-ownership-workspace/fixtures/live-workspace-session.json swift run SibiStudyApp`.

Lo que todavía no está cerrado al 100% es el polish y validación visual de SwiftUI. El loop existe y los tests pasan, pero falta una pasada de producto sobre layout real: capturas del panel con fixture, jerarquía visual más clara para phase/next action, filetree más legible, estados de evaluación menos crudos, mejor separación entre evidencia requerida/seleccionada/citada/faltante, y una revisión de que texto, scrolls y columnas no se rompan en tamaños reales de ventana.

Siguiente paso: hacer un slice exclusivamente UI/SwiftUI de validación visual y refinamiento del workspace nativo, sin cambiar la pedagogía runtime salvo que la UI revele un contrato insuficiente. El agente debería abrir la app con el fixture, capturar/revisar la pantalla Start -> awaiting attempt -> after submit, ajustar la presentación de los tres paneles, agregar identificadores o tests de render donde sea razonable, y cerrar con una nota de reproducción visual que deje claro que el foundation loop no solo existe en contrato, sino que se entiende al mirarlo.
